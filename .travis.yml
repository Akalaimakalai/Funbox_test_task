language: ruby

rvm: 2.6.5

branches:
  only:
    - master
    - travis

services:
  - postgresql

before_script:
  - cp config/database.yml.sample config/database.yml

script:
  - bin/setup
  - RAILS_ENV=test bundle exec rspec
  - bin/startup
